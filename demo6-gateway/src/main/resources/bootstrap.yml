spring:
  application:
    name: demo6-gateway
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER:localhost:8848}
        namespace: ${NACOS_NAMESPACE:}
        file-extension: yaml
        shared-configs:
          - data-id: demo6-common.yaml
            group: DEFAULT_GROUP
            refresh: true
